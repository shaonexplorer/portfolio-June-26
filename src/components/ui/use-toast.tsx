"use client";

/**
 * Simple wrapper around the `sonner` toast library.
 * The project already includes `<Toaster />` in the root layout, so we only need
 * to expose the `toast` function for the form components.
 */
export { toast } from "sonner";
